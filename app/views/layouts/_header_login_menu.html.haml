%li.dropdown
  %a{href: "#", class: "dropdown-toggle", 'data-toggle': "dropdown" }
    家計簿
    %b.caret

  %ul.dropdown-menu
    %li= link_to "一覧", books_path

    %li.dropdown-submenu
      %a{ href: "#" , class: "dropdown-toggle", 'data-toggle': "dropdown"}
        ユーザー別
      %ul.dropdown-menu
        - User.all.each do |user|
          %li= link_to user.name, user_path(user)
    %li.dropdown-submenu
      %a{ href: "#" , class: "dropdown-toggle", 'data-toggle': "dropdown"}
        口座別
      %ul.dropdown-menu
        - Account.all.each do |account|
          %li= link_to account.name, books_account_path(account)
    %li= link_to "カテゴリ別まとめ", report_categories_path
